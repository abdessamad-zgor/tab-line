{
  "name": "tabline_monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "publish": "turbo publish",
    "dev": "turbo dev",
    "build": "turbo build",
    "type-check": "turbo type-check",
    "deploy": "turbo run deploy",
    "deploy:prod": "turbo run deploy:prod"
  },
  "packageManager": "yarn"
}
