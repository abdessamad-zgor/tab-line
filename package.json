{
  "name": "tabline monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "publish": "turbo publish",
    "dev": "turbo dev",
    "build": "turbo build",
    "type-check": "turbo type-check"
  }
}
